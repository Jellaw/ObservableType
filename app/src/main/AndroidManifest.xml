<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.observabletype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ObservableType">
        <activity android:name=".type.FlowableObserverActivity"></activity>
        <activity
            android:name=".type.CompletableCObserverActivity"
            android:label="@string/title_activity_completable_c_observer" />
        <activity android:name=".type.MaybeMaybeObserverActivity" />
        <activity android:name=".type.SingleSingleObserverActivity" />
        <activity android:name=".type.ObservableObserverActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>